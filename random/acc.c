#include <cs50.h>
#include <stdio.h>



int main(void)
{
    int v;
    int i;

        do
        {
            char dir = get_char("direction ? \n");

            if (dir == 'a')
            {

            }
        }

        for (i = 0; i < 12; i = i + 3)
        {
            v = i;
            printf("v = %i\n", v);
        }

        for (int j = i; j >= 0; j = j - 1)
        {
            v = j;
            printf("v = %i\n", v);
        }
}

int dash (string)
{
    
}